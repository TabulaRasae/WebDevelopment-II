<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>BMCC UsedBooks Portal</title>
    <link href="/styles.css" rel="stylesheet" type="text/css" />
  </head>
  <body>
    <div id="main">
      <%- include('partials/navigation'); %>
      <div id="content">
        <div class="hh1">Welcome back, <%= currentUser %></div>
        <p>
          This dashboard lists new arrivals, tracks your reserved books, and
          lets you pick up affordably priced course materials without waiting in
          the campus bookstore line.
        </p>
        <div class="portal-grid">
          <div class="portal-card">
            <h2>Your cart snapshot</h2>
            <p>Subtotal: $<%= totals.subtotal.toFixed(2) %></p>
            <p>Estimated tax: $<%= totals.tax.toFixed(2) %></p>
            <p class="grand-total">
              Grand total: $<%= totals.grandTotal.toFixed(2) %>
            </p>
            <a class="read" href="/cart">view cart</a>
          </div>
          <div class="portal-card">
            <h2>Need assistance?</h2>
            <p>
              The Student Activities Book Committee can help verify ISBNs,
              coordinate on-campus meetups, or answer questions about selling
              your own books.
            </p>
            <a class="read" href="/contact">contact us</a>
          </div>
        </div>
        <div style="height: 15px"></div>
        <h2>Featured textbooks</h2>
        <div class="product-grid">
          <% featuredProducts.forEach(function(product){ %>
          <div class="product-card">
            <img
              src="<%= product.image %>"
              alt="<%= product.name %>"
              class="product-thumb"
            />
            <div class="title"><%= product.name %></div>
            <p class="size"><%= product.shortDescription %></p>
            <div class="product-actions">
              <form action="/cart/add" method="post">
                <input
                  type="hidden"
                  name="productId"
                  value="<%= product.id %>"
                />
                <input type="hidden" name="quantity" value="1" />
                <input type="hidden" name="redirect" value="/portal" />
                <button type="submit" class="submit">Add to Cart</button>
              </form>
              <a class="read" href="/products/<%= product.id %>">details</a>
            </div>
          </div>
          <% }) %>
        </div>
      </div>
      <%- include('partials/footer'); %>
    </div>
  </body>
</html>
