<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Register | BMCC UsedBooks Store</title>
    <link href="/styles.css" rel="stylesheet" type="text/css" />
  </head>
  <body class="auth-page">
    <div class="auth-panel">
      <h1>Create an account</h1>
      <p>
        Register with your BMCC username to list books, reserve titles, and keep
        your cart saved between visits.
      </p>
      <% if (typeof message !== 'undefined' && message) { %>
      <div class="message-banner <%= isError ? 'error' : '' %>">
        <%= message %>
      </div>
      <% } %>
      <form action="/register" method="post" class="auth-form">
        <label
          >Username
          <input type="text" name="userid" required />
        </label>
        <label
          >Password
          <input type="password" name="password" required />
        </label>
        <label
          >Confirm Password
          <input type="password" name="confirmPassword" required />
        </label>
        <button type="submit" class="submit">Register</button>
      </form>
      <p class="auth-switch">Already registered? <a href="/login">Log in</a></p>
    </div>
  </body>
</html>
